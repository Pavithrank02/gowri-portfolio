import { Grid, Typography } from '@mui/material'
import React from 'react'
import HomeDetails from './HomeDetails'


const MainContainer = () => {
  return (
    <div  style={{ position: 'fixed', top: 0, left: 0, width: '100%', height: '100vh', backgroundColor: '#131314', display: 'flex', flexDirection: 'column', alignItems: 'center', justifyContent: 'center', backgroundSize: 'cover', overflow: 'hidden', zIndex: -100 }}>
    </div>
  )
}

export default MainContainer